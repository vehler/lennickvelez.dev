version: 2.1
jobs:
  build:
    working_directory: ~/src
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'npm install'
